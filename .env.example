### Example environment file for local development (DO NOT COMMIT secrets)
# Copy this to `.env` and fill values. Keep `.env` out of version control.

# GitHub token for A.D.E. to create issues (use a minimal-scope PAT or GitHub App token).
# Example: GH_TOKEN=ghp_xxx
GH_TOKEN=

# Target repo for created issues (owner and repo name)
GITHUB_OWNER=johnnywhitemike
GITHUB_REPO=silver-pay
